/**
 * @apitoll/facilitator
 *
 * x402 facilitator service — handles HTTP 402 payment flows
 * for AI agents making micropayments to services.
 *
 * Usage as a standalone server:
 *   npm start
 *
 * Usage as a library (import the Express app):
 *   import { app } from '@apitoll/facilitator';
 */

export { app } from './server.js';
