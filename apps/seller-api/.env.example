# ═══════════════════════════════════════════════════
# API Toll Multi-Tool API — Environment Variables
# ═══════════════════════════════════════════════════

# Required: Your wallet address to receive USDC payments
SELLER_WALLET=0xYourWalletAddress

# Required: x402 facilitator URL (processes USDC payments)
FACILITATOR_URL=https://pay.apitoll.com

# Server port (default: 4402)
PORT=4402

# Public URL for discovery metadata (used in agent discovery headers)
BASE_URL=https://your-seller-api.railway.app

# ── Optional: Enhanced Search ─────────────────────
# Brave Search API (free tier: 2000 queries/month)
# Get key at: https://api.search.brave.com/
BRAVE_API_KEY=

# ── Optional: Convex (for reputation tool) ────────
CONVEX_URL=https://your-convex-deployment.convex.cloud

# ── Optional: Platform Analytics ──────────────────
CONVEX_WEBHOOK_URL=
APITOLL_PLATFORM_KEY=

# ── Optional: Referral ────────────────────────────
REFERRAL_CODE=apitoll-tools

# ── Optional: Redis (for production rate limiting) ──
# Without Redis, rate limiting uses in-memory (single instance only).
# Set REDIS_URL for multi-instance deployments.
REDIS_URL=redis://localhost:6379

# ── Optional: Chain Config Overrides ──────────────
# Base Mainnet (default). Override for testnet:
# USDC_ADDRESS=0x036CbD53842c5426634e7929541eC2318f3dCF7e
# BASE_RPC_URL=https://sepolia.base.org
# NETWORK_ID=eip155:84532

# ── Optional: Nominatim User-Agent ────────────────
NOMINATIM_USER_AGENT=APITollGeoService/1.0
