# ═══════════════════════════════════════════════════
# API Toll — Environment Variables
# ═══════════════════════════════════════════════════
# Copy this file to .env.local and fill in the values.

# ─── Convex (Required) ────────────────────────────
# Set automatically by `npx convex dev` or `npx convex deploy`
CONVEX_DEPLOYMENT=dev:your-deployment-name     # Your Convex deployment name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# ─── Clerk Auth (Required for Dashboard) ──────────
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...  # Clerk → API Keys
CLERK_SECRET_KEY=sk_test_...                   # Clerk → API Keys

# ─── Stripe (Required for Fiat Deposits) ──────────
STRIPE_SECRET_KEY=sk_live_...                  # Stripe → Developers → API Keys
STRIPE_WEBHOOK_SECRET=whsec_...               # Stripe → Developers → Webhooks

# ─── Facilitator (Required for Payments) ──────────
FACILITATOR_PRIVATE_KEY=0x...                  # Private key of USDC hot wallet (Base)
FACILITATOR_API_KEYS=your-secret-key           # Comma-separated API keys for auth
FACILITATOR_URL=http://localhost:3000          # Facilitator service URL

# ─── Blockchain (Base) ───────────────────────────────
BASE_RPC_URL=https://mainnet.base.org          # Base RPC (or Alchemy/Infura endpoint)

# ─── Blockchain (Solana — optional) ─────────────────
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com  # Solana RPC (or Helius/QuickNode endpoint)
SOLANA_PRIVATE_KEY=                             # Solana keypair (base58 or JSON byte array)

# ─── Platform Fee (Revenue Collection) ───────────────
PLATFORM_FEE_BPS=300                              # 300 = 3% (basis points)
PLATFORM_FEE_WALLET_BASE=0x...                    # Base USDC fee recipient wallet
PLATFORM_FEE_WALLET_SOLANA=                       # Solana USDC fee recipient (optional)

# ─── Security ─────────────────────────────────────
ALLOWED_ORIGINS=https://apitoll.com,http://localhost:3333

# ─── Optional ─────────────────────────────────────
REDIS_URL=redis://localhost:6379               # For seller-sdk rate limiting
LOG_LEVEL=info                                 # debug, info, warn, error
NODE_ENV=development                           # development, production, test
PORT=3000                                      # Server port
