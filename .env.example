# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=
CONVEX_URL=
CONVEX_SITE_URL=

# Platform Configuration
PLATFORM_FEE_BPS=300                    # 3% platform fee (basis points)
PLATFORM_WALLET_BASE=                   # Platform USDC wallet on Base
PLATFORM_WALLET_SOLANA=                 # Platform USDC wallet on Solana

# Stripe (for subscription billing)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Security
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# PostgreSQL (for indexer/discovery)
DATABASE_URL=postgresql://user:pass@localhost:5432/agentcommerce

# Optional
NODE_ENV=development
